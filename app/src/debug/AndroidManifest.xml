<?xml version="1.0" encoding="utf-8"?>
<!-- Jen v debug buildu: náhled obrazovek s ukázkovými daty (do vydané appky se nedostane) -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name=".DesignPreviewActivity"
            android:exported="true"
            android:theme="@style/Theme.CannaApp" />
    </application>
</manifest>
